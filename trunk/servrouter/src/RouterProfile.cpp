#include <RouterProfile.h>

RouterProfile::RouterProfile() {
	//mCreators.push_back(std::pair<int, TransferableCreator*>(, ));
	
	//mCreatorIds.push_back(std::pair<std::string, int>(, ));
}

RouterProfile::~RouterProfile() {
	
}

